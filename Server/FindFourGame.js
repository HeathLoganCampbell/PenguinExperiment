// FindFour
    // Join
    // Start
    // turn
    // Place_token
    // GameOver
    // Leave

function onEvent(socket, action, payload)
{
    if(action == 'findfour_join')
    {
        
    }

    if(action == 'findfour_leave')
    {
        
    }
}